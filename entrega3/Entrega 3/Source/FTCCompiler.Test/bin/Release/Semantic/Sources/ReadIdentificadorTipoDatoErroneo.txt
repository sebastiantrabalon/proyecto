const M : integer = 7;
var A : integer;
var B : boolean;

procedure MAIN();
var A: boolean;
begin
  while B do
    if A = 0 then
      A := A + 1;
    end-if;
    showln 'Visualizacion', M, ' ', A;
    A := M - 1;
  end-while;
  read B;
end-proc;